services:
  sgb-api:
    build: .
    container_name: saenggibu-api
    restart: unless-stopped
    ports:
      - "8787:8787"
    env_file:
      - .env
    volumes:
      - ./data/saenggibu:/app/data/saenggibu
